# macOS Music Agent

这个目录用于放置 macOS 专属的 `music-agent` 方案、脚本和后续实现文件。

当前已收录：

- `MACOS_VOICE_CONTROL_PLAN.md`

用途：

- 作为 macOS 语音控制能力的独立工作区
- 避免和现有 Windows / 通用实现混在一起
- 后续可以继续在这里补：
  - `start-voice.sh`
  - `macos-voice-listener.ts`
  - `macos-speech-bridge.swift`
